#!/bin/bash

sudo update-alternatives --remove-all fsc
sudo update-alternatives --remove-all scala
sudo update-alternatives --remove-all scalac
sudo update-alternatives --remove-all scaladoc
sudo update-alternatives --remove-all scalap